services:
  # Nginx + Flask統合サービス
  - type: web
    name: babysitter-app
    runtime: docker
    repo: https://github.com/haginot/babysitter-claim-city-suginami
    branch: feature/table-editor
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 10000
